#include "../../../VisOS.Driver/VisOS.TestDevice/test_device.vhl"

string(str_test_name, "Var1ArgsTest");
string(str_ret_val_fail, "Return value is not 5");

private var TestC(var arg)
{
	return arg;
}

private var TestV(var arg)
{
	var b = arg;
	return b;
}

private void VarV1ArgsTest()
{
	BeginTest(ptr_of(str_test_name), size_of(str_test_name));
	var ar = 5;
	var a = TestC(ar);
	if(a == 5)
	{
		var c = TestV(ar);
		if(c == 5)
		{
			PassTest();
			return;
		}
	}
	FailTest(ptr_of(str_ret_val_fail), size_of(str_ret_val_fail));
	return;
}

private void VarC1ArgsTest()
{
	BeginTest(ptr_of(str_test_name), size_of(str_test_name));
	var a = TestC(5);
	if(a == 5)
	{
		var c = TestV(5);
		if(c == 5)
		{
			PassTest();
			return;
		}
	}
	FailTest(ptr_of(str_ret_val_fail), size_of(str_ret_val_fail));
	return;
}
public void Var1ArgsTest()
{
	VarC1ArgsTest();
	VarV1ArgsTest();
	return;
}

Var1ArgsTest();