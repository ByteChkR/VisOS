#include "VisVM/hfs.vhl"

string(bootfile, "../Example/RunOS.vbin");
const_var bootStart = 4096;

var bootSize = HFS_GetFileSize(ptr_of(bootfile), size_of(bootfile));
var boot = ptr_of(bootStart);

HFS_ReadAllData(ptr_of(bootfile), size_of(bootfile), boot, bootSize);

boot();